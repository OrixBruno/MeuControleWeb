
@{
    ViewBag.Title = "Impressao";
}
<div id="divCarregando" class="progresso">
    <img src="@Url.Content("~/Content/images/carregando.gif")" />
    <br />
    Carregando...
</div>

<div class="container">
    <h2>Impressao</h2>
    @using (Ajax.BeginForm("ConsultarMapa", "Mapa", new AjaxOptions
    {
        HttpMethod = "GET",
        OnSuccess = "Sucesso",
        OnFailure = "Error",
        InsertionMode = InsertionMode.Replace,
        LoadingElementId = "divCarregando",
        UpdateTargetId = "divResultadoConsulta"
    }, new { @class = "form-inline" }))
    {
    <div class="form-group">
        <label>Letra</label>
        <input id="letra" name="letra" placeholder="A" class="form-control" />
    </div>
    <div class="form-group">
        <label>Código</label>
        <input id="codigo" name="codigo" placeholder="0" class="form-control" />
    </div>

    <input type="submit" id="buscar" name="buscar" value="Buscar" class="btn btn-danger" />
    }
</div>

<div id="divResultadoConsulta" class="margin-top-10">

</div>
@section ScriptsImpressao{
    <script>
        function Sucesso(resposta) {
            
        }
    </script>
}

